/** Fraud detection thresholds — used by transaction processing pipeline */
export const DUPLICATE_MIN_AMOUNT = 50;
export const ROUND_NUMBER_THRESHOLD = 500;
export const ROUND_NUMBER_MODULO = 100;
export const DUPLICATE_TOLERANCE = 0.05;
